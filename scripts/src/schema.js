/**
* Parse database schema. Automatically generated into models by modlify.js.
*/

'use strict';

// Each class (model) should be a new object within schema.
let schema = {
	Test: {
		t1: _.isString,

	}
};
